# GoReleaser config for cross-platform binary releases.
# https://goreleaser.com
#
# Run locally:   goreleaser release --snapshot --clean
# CI release:    triggered by `git tag v0.X.Y && git push --tags`

version: 2

project_name: beamd

before:
  hooks:
    - go mod tidy

builds:
  - id: beamd
    main: ./cmd/beamd
    binary: beamd
    env:
      - CGO_ENABLED=0
    goos: [linux, darwin]
    goarch: [amd64, arm64]
    flags: [-trimpath]
    ldflags:
      - -s -w -X main.Version={{.Version}}

  - id: beam-testapp
    main: ./cmd/beam-testapp
    binary: beam-testapp
    env:
      - CGO_ENABLED=0
    goos: [linux, darwin]
    goarch: [amd64, arm64]
    flags: [-trimpath]
    ldflags:
      - -s -w

archives:
  - id: archives
    name_template: >-
      {{ .ProjectName }}_{{ .Version }}_{{ .Os }}_{{ .Arch }}
    formats: [tar.gz]
    files:
      - README.md
      - LICENSE
      - example/beamd.yaml
      - scripts/smoke-test.sh
      - docs/setup.md
      - docs/post-manual-testing.md

checksum:
  name_template: checksums.txt

snapshot:
  version_template: "{{ incpatch .Version }}-next"

changelog:
  sort: asc
  filters:
    exclude:
      - "^docs:"
      - "^test:"
      - "^chore:"
      - merge conflict
      - Merge pull request
      - Merge branch

dockers:
  - id: beamd-image
    ids: [beamd]
    image_templates:
      - "ghcr.io/dynamismlabs/beamd:{{ .Version }}"
      - "ghcr.io/dynamismlabs/beamd:latest"
    dockerfile: Dockerfile
    use: buildx
    build_flag_templates:
      - --platform=linux/amd64
      - --label=org.opencontainers.image.title=beamd
      - --label=org.opencontainers.image.description=Self-hostable instant-URL tunnel server
      - --label=org.opencontainers.image.source=https://github.com/dynamismlabs/beamd
      - --label=org.opencontainers.image.licenses=Apache-2.0
      - --label=org.opencontainers.image.version={{ .Version }}

release:
  draft: true
  prerelease: auto
